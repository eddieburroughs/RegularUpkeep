# Test Environment Configuration
# Copy to .env.test for local test runs
#
# Two modes:
# 1. UNIT TESTS: Use mock values (default) - tests use vitest mocks
# 2. RLS SECURITY TESTS: Use real Supabase credentials (see below)

# === SUPABASE (Unit tests - mocked) ===
NEXT_PUBLIC_SUPABASE_URL=https://test.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key

# === SUPABASE RLS SECURITY TESTS ===
# For `npm run test:rls` - tests run against real Supabase instance
# WARNING: These tests create/delete data. Use test-specific emails!
#
# Uncomment and fill in for RLS testing:
# SUPABASE_TEST_URL=https://api.regularupkeep.com
# SUPABASE_TEST_ANON_KEY=your_real_anon_key
# SUPABASE_TEST_SERVICE_ROLE_KEY=your_real_service_role_key

# === TEST USER CREDENTIALS ===
# Used by RLS tests to create and authenticate test users
# These should be unique emails that won't conflict with production
TEST_HOMEOWNER_A_EMAIL=test-homeowner-a@regularupkeep-test.local
TEST_HOMEOWNER_A_PASSWORD=TestPassword123!
TEST_HOMEOWNER_B_EMAIL=test-homeowner-b@regularupkeep-test.local
TEST_HOMEOWNER_B_PASSWORD=TestPassword123!
TEST_PROVIDER_EMAIL=test-provider@regularupkeep-test.local
TEST_PROVIDER_PASSWORD=TestPassword123!
TEST_HANDYMAN_EMAIL=test-handyman@regularupkeep-test.local
TEST_HANDYMAN_PASSWORD=TestPassword123!
TEST_ADMIN_EMAIL=test-admin@regularupkeep-test.local
TEST_ADMIN_PASSWORD=TestPassword123!

# AI Configuration (mocked in tests)
OPENAI_API_KEY=sk-test-mock-key
ANTHROPIC_API_KEY=sk-ant-test-mock-key

# Email (mocked in tests)
RESEND_API_KEY=re_test_mock_key

# Cron Jobs (not used in unit tests)
CRON_SECRET=test-cron-secret

# Push Notifications (not used in unit tests)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=test-vapid-public
VAPID_PRIVATE_KEY=test-vapid-private

# SMS (not used in unit tests)
TWILIO_ACCOUNT_SID=test-twilio-sid
TWILIO_AUTH_TOKEN=test-twilio-token
TWILIO_PHONE_NUMBER=+15551234567
